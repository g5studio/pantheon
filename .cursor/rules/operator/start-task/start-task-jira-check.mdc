---
description: 在 start-task 檢查 Jira 單時需要遵守的原則，包含子任務偵測和優先檢查 RD 提供的建議
alwaysApply: false
---

# Start-Task Jira Ticket 檢查原則

此規則只保留 `start-task` 讀 Jira 時的判斷原則；主流程與模式差異請依對應文件。

## 核心原則 1：先檢測是否為子任務

讀取 Jira ticket 後，**第一優先級**就是檢測是否為 `Sub-task`。

檢測規則：

1. 檢查 `fields.issuetype.name`
2. 若為 `Sub-task`，從 `fields.parent.key` 取得父任務單號

處理結果：

| 條件 | 分支單號 | Commit 單號 | MR 關聯單號 |
|---|---|---|---|
| `issuetype.name === "Sub-task"` | 父任務單號 | 子任務單號 | 子任務 + 父任務 |
| 其他 | 用戶提供的單號 | 用戶提供的單號 | 用戶提供的單號 |

若偵測到子任務，必須在 chat 顯示：

```text
🔍 正在檢測 ticket 類型...
📌 偵測到此為子任務（Sub-task）
📋 子任務單號: {SUB_TASK}
📋 父任務單號: {PARENT_TICKET}

ℹ️ 分支將使用父任務單號: feature/{PARENT_TICKET}
ℹ️ Commit 將使用子任務單號: ({SUB_TASK})
ℹ️ MR 將關聯兩個單號: {SUB_TASK}, {PARENT_TICKET}
```

禁止：

- 用子任務單號建立分支
- MR 只關聯子任務不關聯父任務
- 偵測到子任務後不在 chat 顯示結果

## 核心原則 2：優先檢查 RD 建議

在分析 Jira ticket 時，必須優先檢查是否有 RD 提供給 AI 的建議。

檢查位置：

1. Description
2. Comments
3. Summary

搜尋格式（不分大小寫）：

- `for AI`
- `to AI`
- `AI suggestion`
- `AI suggestion:`
- `for AI:`
- `to AI:`
- `@AI`

如果找到 RD 建議：

- 在顯示 ticket 資訊時優先顯示
- 納入開發計劃的優先考量
- 清楚區分哪些是 RD 建議，哪些是 AI 推斷

若未找到，才按標準流程分析 ticket。

## 顯示格式

當找到 RD 建議時，應優先以此格式顯示：

```text
📋 Jira Ticket 信息
============================================================
單號: {TICKET}
標題: {SUMMARY}
類型: {ISSUE_TYPE}
狀態: {STATUS}

🎯 RD 建議（優先處理）
============================================================
{RD_SUGGESTION}

🎯 初步開發計劃
============================================================
1. {STEP_1}
2. {STEP_2}
3. {STEP_3}
```

## 階段檢查聲明

AI 在 `start-task` 流程中，仍必須保留階段檢查語義，避免跳過關鍵步驟。

輸出原則：

- 若 `start-task.md` 已要求輸出對應的標準資訊區塊，應**優先合併在同一則回覆中完成**
- 不要為了補一個「階段檢查聲明」再額外拆出獨立前言、獨立確認句、或重複的 ticket/計劃摘要
- 只有在標準區塊尚未涵蓋必要檢查資訊時，才補充階段檢查聲明
- 目標是保留檢查點語義，不是增加額外 chat turn 或重複文案

### 階段 1：讀取 Jira 後

```text
⚠️ Start-Task 階段 1 檢查：

📋 已讀取 Ticket 信息：
   單號: {TICKET}
   標題: {SUMMARY}
   類型: {ISSUE_TYPE}

🔍 RD 建議檢查結果：
   [✓] 已檢查 Description 中的 "for AI" / "to AI" 標記
   [✓] 已檢查 Comments 中的 "AI suggestion" 標記
   結果: {FOUND / NOT_FOUND}

🎯 初步開發計劃：
   1. {STEP_1}
   2. {STEP_2}
   ...

❓ 請確認此開發計劃是否正確？
```

### 階段 2：開發完成後

```text
⚠️ Start-Task 階段 2 檢查：

📋 開發完成檢查：
   [✓] 已完成所有開發步驟
   [✓] 已保存開發計劃
   [✓] 變更檔案: {FILES_CHANGED}

📝 本次修改摘要：
   - 新增: {NEW_FILES}
   - 修改: {MODIFIED_FILES}
   - 刪除: {DELETED_FILES}

❓ 請確認修改是否正確？確認後將執行 cr 命令。
```

### 階段 3：建立 MR 後

```text
⚠️ Start-Task 階段 3 檢查：

📋 MR 建立檢查：
   [✓] MR Description 包含開發計劃
   [✓] MR Description 包含問題分析（如為 Bug）
   [✓] 已添加 AI label
   [✓] 已保存 start-task 資訊

✅ MR 建立成功：{MR_LINK}
```

## 最終檢查要求

在完成 Jira 分析並進入下一階段前，必須確認以下事項都已完成：

- 已檢查 `fields.issuetype.name`
- 若為子任務，已提取 `fields.parent.key`
- 已在 chat 顯示子任務偵測結果
- 已優先檢查 Description / Comments 中的 RD 建議
- 已在計劃中優先納入 RD 建議
- 已輸出對應階段的檢查聲明

若上述任一項未完成，必須先補齊，不可直接繼續後續流程。
