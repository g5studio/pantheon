---
description: 定義 operator 在 start-task 程序時需遵守的開發原則，包含開發流程、報告生成和 MR 處理
alwaysApply: false
---

# Start-Task 開發原則

此規則只保留 `start-task` 共享的開發 / 保存 / 交付原則；模式差異請回到各 mode rule。

## 相關規則

- [operation-modes.mdc](mdc:.cursor/rules/operator/start-task/operation-modes.mdc)
- [start-task-jira-check.mdc](mdc:.cursor/rules/operator/start-task/start-task-jira-check.mdc)
- [commit-and-mr-guidelines.mdc](mdc:.cursor/rules/cr/commit-and-mr-guidelines.mdc)

## 模式如何影響流程

| 原則 | 謹慎模式 | 多工模式 | 快速模式 | 預設模式 |
|---|---|---|---|---|
| 確認流程 | 每步確認 | 計劃確認 | 僅排程確認 | 每階段確認 |
| Commit 時機 | 單次 | 每階段 | 每任務 | 單次 |
| MR 策略 | 單一 MR | 單一 MR | 每任務 MR | 單一 MR |

模式差異細節以 `operation-modes.mdc`、`fast-mode.mdc`、`multi-task-mode-sop.mdc` 為準。

## 保存開發計劃

當用戶確認開發計劃後，必須立即保存計劃資訊，作為後續開發與 MR 的來源資料。

至少要保存：

- `ticket`
- `summary`
- `issueType`
- `status`
- `assignee`
- `priority`
- `suggestedSteps`
- `sourceBranch`
- `featureBranch`
- `startedAt`
- `operationMode`
- `modeConfig`
- `aiCompleted`
- `developmentReport`

保存方式：

- 以 repo 內既有的 `save-start-task-info.mjs` / 對應流程為準
- 用戶確認開發計劃後必須**立即**保存
- 保存後必須執行驗證流程，確認資料存在
- 保存成功前不得開始開發
- 在 commit / MR 前若發現資料不存在，必須停止並補齊
- 若計劃尚未得到用戶確認，必須保持暫停，不得保存後直接往下推進

chat 輸出原則：

- 保存 / 驗證是必要內部流程，但**不需要**因成功而額外新增 chat turn
- 若無阻塞，應直接進入下一個本來就需要對用戶顯示的步驟
- 只有在保存失敗、驗證失敗、資料不一致、或需要用戶補充資訊時，才在 chat 中明確回報

最少應完成以下檢查點：

- 用戶確認開發計劃後立即保存
- 開發完成準備 commit 前檢查資料存在
- 執行 `cr` 指令前再次驗證資料存在

若在 commit / MR 階段才發現資料遺漏：

1. 立即停止
2. 詢問用戶是否補充計劃資料
3. 補齊後再繼續交付流程

## 開發執行原則

- 不可超出已確認範圍
- 發現額外修正需求時先回報
- 高風險變更先詢問
- 謹慎模式不可順便優化
- 多工模式按排程逐步推進
- 快速模式在授權點後可自動推進，但遇到停止點仍要停
- AI 獨立完成的 `start-task` 項目，必須標記 `aiCompleted: true`

## 開發完成後

在進入 commit / MR 前，必須先整理：

- 當前分支
- ticket
- 已完成步驟
- 主要修改檔案
- 修改摘要

所有模式都必須在進入交付流程前等待用戶確認；快速模式可在排程確認後自動推進開發，但最終仍必須以集中摘要 / 報告取得用戶確認後才能進入 `cr`。

當 AI 使用 `start-task` 完成修改後，仍必須在 chat 中顯示完整任務摘要與開發報告，等待用戶確認後才能執行 `cr` 建立 MR。

在用戶明確確認前，必須保持暫停，不可自行進入 commit / MR。

輸出原則：

- 以 `start-task.md` 要求的最終修改摘要區塊為主
- 不要在最終摘要前，額外插入僅用於回報「已保存」「已驗證」「已同步」等內部成功狀態的獨立訊息
- 若內部持久化流程正常，將結果反映在最終摘要 / 後續交付即可

## 報告確認後的交付銜接

當用戶確認最終開發報告後，**不可直接跳到 `cr`**；必須先完成報告持久化與交付銜接。

必須依既有 repo 流程完成：

1. 將最終確認版本的開發報告寫回 `start-task` 的資料來源
2. 若 repo 使用既有腳本 / 流程（例如 `update-development-report` 類流程），必須先執行
3. 確認後續 MR 建立流程會讀到**同一份最終版本**
4. 完成上述步驟後，才可呼叫 `cr single-ticket` / `cr multiple-ticket`

如果報告尚未寫回、版本不一致、或無法確認 MR 會讀到最新版本，必須停止並先處理這個問題。

## Development Report

建立 MR 前，development report 至少要包含：

- `## 📋 關聯單資訊`
- `## 📝 變更摘要`
- `### 變更內容`
- `## ⚠️ 風險評估`

額外要求：

- `Bug`：補影響範圍 / 根本原因 / 造成問題的單號 / 改動前後邏輯差異
- `Request`：補需求覆蓋 / 預期效果 / 潛在影響風險報告
- 最終格式與交付規則以 `commit-and-mr-guidelines.mdc` 為準

若此項目由 `start-task` 啟動並主要由 AI 完成，建立 MR 時必須保留原本行為：

- 明確包含 `AI` label，不可依賴腳本靜默補齊
- 檢附最終確認版本的開發計劃 / 報告到 MR description
- 若開發方向中途有調整，MR 內容必須以最終確認版本為準
- 報告必須先完成持久化，再交由 `cr` 流程建立 MR

## 停止條件

以下情況必須停下：

- 開發計劃尚未獲得必要確認
- 開發計劃資料未保存
- 需要修改受保護檔案但尚未授權
- Jira / GitLab 必備資訊不足
- 發現需求與分支 / ticket 對不上

## 最終確認要求

在離開 `start-task` 流程前，必須再次確認以下事項都已成立：

- 已依所選模式執行正確的確認頻率
- 已保存開發計劃與模式資訊
- 已在開發完成後先做摘要回報
- 已在 MR 前補齊 development report

若上述任一項尚未成立，必須先停止並補齊，不可直接進入下一階段。
