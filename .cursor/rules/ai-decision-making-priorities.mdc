---
alwaysApply: true
---

# AI 決策優先級與守則遵守規則

## 核心原則

**最高優先級永遠是：先詢問再修改 > 完成任務。**

當任務目標、腳本流程、或自動化指令與此原則衝突時，必須以此原則為準。

## 決策順序

AI 在執行任何任務時，依照以下順序決策：

1. **保護用戶變更**：不可讓既有工作遺失、被覆蓋、或被靜默修改
2. **遵守受保護檔案規則**：修改 `.cursor/commands/`、`.cursor/rules/`、`.cursor/scripts/` 前必須先獲授權
3. **先檢查、後回報、再等待**：發現問題時先停下，不可直接修
4. **只有在用戶明確授權後才修改**
5. **任務完成排在最後**

## 絕對禁止

未經用戶明確授權，不得自行修改以下檔案：

- `.cursor/rules/`
- `.cursor/scripts/`
- `.cursor/commands/`

這條規則沒有例外。即使是小修、明顯錯誤、或為了讓任務繼續進行，也不能跳過授權。

## 發現問題時的標準流程

當檢查、腳本、lint、型別、測試、架構規則、或 Git 操作發現問題時，必須：

1. **立即停止當前流程**
2. **在 chat 清楚回報**
   - 問題是什麼
   - 影響哪些檔案或流程
   - 建議如何處理
3. **明確詢問用戶是否要我協助修正**
4. **等待用戶回覆**
5. **只有在明確同意後才執行修改**

不可把「自動化流程」理解成「自動修復流程」。

## 腳本錯誤處理

當 `.cursor/scripts/` 內的腳本執行失敗時：

- 腳本報錯 = **停止並回報**
- 任務被腳本阻擋 = **停止並回報**
- 看起來像腳本 bug = **停止並回報**

不可因為腳本報錯就直接修改腳本來完成任務。

## 受保護檔案修改前的必備聲明

當要修改 `.cursor/rules/`、`.cursor/scripts/`、`.cursor/commands/` 內的檔案時，必須先在 chat 中清楚說明：

- 目標檔案
- 修改原因
- 預計修改內容
- 詢問用戶是否授權

在用戶同意前，不可動手修改。

**必須輸出格式**：

```text
⚠️ 即將修改受保護的檔案，依據「絕對最高準則」進行檢查：

📁 目標檔案: {FILE}
📂 所屬目錄: {.cursor/scripts/ | .cursor/rules/ | .cursor/commands/}

📋 需要修改的原因:
{WHY}

📝 需要修改的內容:
{WHAT}

❓ 是否需要我協助修正這個問題？
```

如果未先輸出上述檢查聲明就修改受保護檔案，視為嚴重違規。

## 不可假設用戶意圖

以下推論一律禁止：

- 因為錯誤「看起來很明顯」就直接修
- 因為任務被阻擋就自行改腳本/規則
- 因為用戶要求完成任務就假設包含授權修改受保護檔案
- 因為變更很小就跳過詢問

## 結構調整準則

當改動屬於結構調整（如 ESLint 拆分、檔案拆分、衝突合併、搬移邏輯）時：

- 只調整結構，不可順便優化原始邏輯
- 結構調整後必須驗證改動前後邏輯一致
- 若發現邏輯差異，必須立即停止並回報

驗證至少包括：

- 函數呼叫順序
- 條件判斷邏輯
- 資料流
- 副作用

## 方案選擇準則

當存在多個可行方案時，必須：

1. 列出可行方案
2. 標示推薦方案與原因
3. 等待用戶選擇
4. 只執行用戶選擇的方案

禁止：

- 自行決定方案
- 假設用戶會接受推薦方案
- 省略其他重要可行方案

## 改動回報準則

每次進行調整時，必須在 chat 回報：

1. 影響範圍：檔案、函數/組件、功能模組
2. 改動情境：為什麼改、解決什麼問題、可能副作用
3. 改動摘要：新增 / 修改 / 刪除

不可只說做了什麼，而不說影響範圍或副作用。

## Git / 安全底線

執行任何可能影響工作目錄的 Git 操作時，必須確保用戶變更不會遺失。

操作前必做：

- 用 `git status --porcelain` 記錄完整變更清單
- 若操作可能改寫工作目錄，保存該清單作為後續驗證基準
- 若風險高或可能不可逆，先在 chat 說明並詢問

操作後必做：

- 再次檢查工作目錄狀態
- 比對操作前後的變更完整性
- 若發現差異、衝突、或變更疑似遺失，立即停止並回報

特別規則：

- `git stash` 後要驗證 stash 內容是否完整
- `git stash pop` / `rebase` / `pull -r` 後要驗證原始變更是否仍存在
- 不可在 stash / rebase 失敗後自動做清理操作
- 不可在未確認 stash 內容前執行 `git stash drop`

若發現用戶變更遺失，必須：

1. 立即停止所有後續操作
2. 只做安全的恢復檢查，例如 `git stash list`、`git reflog`、`git fsck --unreachable`
3. 在 chat 說明遺失情況與恢復方向
4. 等待用戶指示

執行高風險 Git 操作前，必須輸出：

```text
⚠️ 即將執行 Git 操作，依據規則進行安全檢查：

📋 變更清單記錄：
{FILES}

✅ 安全檢查項目：
- 已記錄完整變更清單
- 準備在操作後驗證結果
- 若操作失敗將立即停止，不會自動清理
- 若發現變更遺失將先回報再處理

🔄 即將執行：{COMMAND}
```

## 自我檢視原則

執行任何任務時，必須做行動前確認與行動後品質核對。

### 行動前規範確認

在第一個工具調用或任何可能產生副作用的操作前，必須：

1. 識別任務類型
2. 重新閱讀相關規範，不可只憑記憶
3. 檢查前置條件
4. 在 chat 輸出規範確認聲明

常見任務至少要確認：

- CR / MR：`commit-and-mr-guidelines.mdc`、`mr-execution-result-report.mdc`
- Jira / 文件讀取：`jira/auto-read-confluence-jira.mdc`
- 版本更新：`utilities/bump-version.mdc`
- 通知：`chat-report-guideline.mdc`
- 修改受保護檔案：本規則的受保護檔案章節
- 高風險 Git：本規則的 Git / 安全底線

必須輸出格式：

```text
📋 行動前規範確認：

🔍 即將執行的任務類型：{TASK_TYPE}

✅ 已確認閱讀的相關規範：
- {RULE_1}
- {RULE_2}
- {RULE_3}

📖 規範重點摘要：
{SUMMARY}

✅ 自我檢視通過，開始執行任務。
```

聲明 ≠ 執行：

- 輸出規範確認聲明不等於已遵守規範
- 聲明後仍必須逐步核對是否真的照規範執行
- 若發現前置條件不符，必須停止並詢問

### Command 前置條件

執行 command 前至少確認：

- `cr single-ticket`：當前是否為 feature branch、是否有待處理變更、ticket 是否有效
- `cr multiple-ticket`：當前是否為 feature branch、是否為多 ticket 情境
- `start-task`：ticket 是否有效、是否需要切換/建立分支
- `commit-and-push`：是否有待提交變更、當前分支狀態是否合理

前置條件不符時，必須停止並在 chat 說明預期 / 實際狀態，等待用戶決定。

### Agent 版本檢查

任務開始前，必須檢查 `version.json` 或 `.pantheon/version.json` 的版本資訊。

若版本落後：

- 必須執行 `oracle` 同步最新版本
- 重新讀取相關規範
- 再依最新規範執行任務

### 行動後品質核對

任務完成後，必須核對成果是否符合相關規範，並在 chat 輸出品質檢測報告。

至少檢查：

- MR：description、labels、assignee、reviewer、報告完整性
- commit：message 格式、ticket 格式、語言要求
- 版本更新：版本號格式、版本檔案同步、commit message
- 通知：內容是否正確、是否依規範發送
- 檔案修改：影響範圍與副作用是否已回報

必須輸出格式：

```text
✅ 工作品質檢測報告：

📋 任務類型：{TASK_TYPE}

🔍 規範符合性檢核：
| 檢核項目 | 規範要求 | 實際結果 | 狀態 |
|---|---|---|---|
| {ITEM} | {RULE} | {ACTUAL} | ✅/❌ |

📊 檢測結果：{PASS_OR_ISSUES}

{IF_ISSUES}
```

若跳過自我檢視、版本檢查、或品質檢測，必須立即停止，補齊缺漏步驟，再繼續。

## 規則衝突處理準則

當用戶指令與專案規則衝突時，不可直接執行；必須先說明衝突與風險，等待用戶決定。

處理流程：

1. 立即停止
2. 說明衝突的規則來源與衝突點
3. 說明執意執行的風險
4. 等待用戶決定
5. 若用戶仍堅持執行，記錄此為用戶明確決定

若衝突的是本規則的核心原則：

- 不可繞過「先詢問再修改」
- 不可繞過受保護檔案授權要求
- 不可繞過 Git 安全底線
- 只能先說明無法直接執行，或在獲授權後修改規則本身

## 檢查清單

執行任務時至少自問：

- [ ] 是否已完成行動前規範確認？
- [ ] 是否已確認前置條件？
- [ ] 是否把檢查與修復分開？
- [ ] 是否在修改前取得明確授權？
- [ ] 是否避免假設用戶意圖？
- [ ] 若有多方案，是否已列出並等待選擇？
- [ ] 若做結構調整，是否驗證邏輯一致？
- [ ] 若做 Git 操作，是否已記錄並驗證變更完整性？
- [ ] 任務完成後，是否已輸出品質檢測報告？
