---
alwaysApply: true
---

# Pantheon 路徑規則

此規則只定義 Pantheon 在掛載專案中的**通用路徑解析**；若某個 workflow 文件已經完整定義了自己的 path contract（例如 commit / MR workflow），優先遵守該 workflow 文件，不必再重讀本規則。

## 核心對應

當 Pantheon 掛載到其他專案時：

- `.cursor/rules/...` -> `.pantheon/.cursor/rules/...`
- `.cursor/scripts/...` -> `.pantheon/.cursor/scripts/...`
- `.cursor/commands/...` -> `.pantheon/.cursor/commands/...`
- `.cursor/version.json` -> `.pantheon/.cursor/version.json`

## 通用腳本路徑判斷

當 AI 需要執行 Pantheon 腳本時，必須依序用**檔案存在**判斷：

1. `.pantheon/.cursor/scripts/...`
2. `.cursor/scripts/...`
3. `.cursor/scripts/prometheus/...`

不要只靠搜尋結果，也不要假設 `.pantheon/` 一定存在或一定不存在；在 Pantheon 專案本身通常會落到 `.cursor/scripts/...`。

腳本在規則 / 命令 / 文件中的表示方式，仍優先使用原本的邏輯路徑，例如：

- `cr/create-mr.mjs`
- `.cursor/scripts/cr/create-mr.mjs`

不要把 mounted 實體路徑當作新的腳本標識方式。

## 重要限制

- 不要用搜尋腳本檔名來定位 Pantheon 腳本
- 不要假設 `.pantheon/` 一定存在或一定不存在
- 若某 workflow 已明確規定 path resolution，直接遵守 workflow 文件
- 在掛載專案中，不要再額外讀取任何獨立外部副本

## 診斷方式

若路徑仍無法確認，可用：

```bash
if [ -f ".pantheon/.cursor/scripts/<path>" ]; then
  echo "found: .pantheon"
elif [ -f ".cursor/scripts/<path>" ]; then
  echo "found: .cursor"
elif [ -f ".cursor/scripts/prometheus/<path>" ]; then
  echo "found: prometheus symlink"
fi
```

必要時再用 `ls -la` 直接列目錄，不要依賴搜尋工具的空結果來判斷腳本不存在。
